apply plugin: 'application'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile project(":core")
    groovy localGroovy()
}
mainClassName = "Main"

run {
    args "$rootDir/shortcuts","$rootDir/static-app/shortcuts"
}